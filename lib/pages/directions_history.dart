import 'package:flutter/material.dart';

class DirectionsHistoryPage extends StatelessWidget {
  const DirectionsHistoryPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Directions history page'),
    );
  }
}
